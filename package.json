{
  "name": "ever-after-digital",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build:app": "npm --prefix app ci && npm --prefix app run build",
    "build:storefront": "npm --prefix storefront ci && npm --prefix storefront run build",
    "build": "npm run build:app && npm run build:storefront",
    "dev:app": "npm --prefix app run dev",
    "dev:storefront": "npm --prefix storefront run dev"
  }
}
